# aaxview
Minimalist image viewer. Pretty self-explanatory.

## Installation
Run the install script...

## Usage
`aaxview <filename>` or use the desktop entry. If run with no arguments, re-opens the most recent file.

## Configuration
Press `P` to open the settings menu.
